{
    "name": "fastapi + vue",
    "version": "0.1.0",
    "private": true,
    "engines": {
      "node": "16.x"
    },
    "scripts": {
      "heroku-prebuild": "rm -rf node_modules && cd frontend && rm -rf node_modules && cd ..",
      "start": "cd backend && ./bin/start.sh",
      "build": "cd frontend && npm install && npm run build && cd ..",
      "heroku-cleanup": "rm -rf node_modules && cd frontend && rm -rf node_modules && cd .."
    },
    "dependencies": {
    },
    "devDependencies": {
    },
    "postcss": {
      "plugins": {
        "autoprefixer": {}
      }
    },
    "browserslist": [
      "> 1%",
      "last 2 versions",
      "not ie <= 10"
    ]
  }
